# Review — 2026-08-11T20:58:13Z

Reviewer: `workflows.reviewer`
Outcome: **changes requested**

The dependency migration is technically sound and ready for certification after
the two bounded cleanup items below. I independently reviewed the current diff,
the generated lock movement, the implementer's verification evidence, and the
failed run's source-rebuild resolution path.

## Verified

- All three manifest files contain the required four `web-rest` declarations at
  range `0.7`.
- The three locks contain exactly four direct `web-rest@0.7.0` pins with package
  hash `1d34159f0ede4a79c730a176e454165fa29e868fa3a8289024e869799751298c`
  and source content id
  `sha256:f59004d190744de7eda4151fb626d27fb06ab4c2835394dc8edfdc19af964071`;
  no unrelated lock dependency moved.
- `uflowsd` and `RUNNER_VERSION` agree at `0.8.1`, and the new uflowsd claim
  carries the corresponding `web-rest 0.7` dependency and changed source content
  identity.
- The current source compiles for `microflows-runner`, `uflowsd`, and the
  participant stub against the failed run's staged `web-rest@0.7.0`; the real
  runner and daemon binaries both report `0.8.1`.
- The focused integration lane is green: 231/231 tests, including the four
  draining/503 cases identified in the first review. No existing test was edited.
- I independently ran `drift lock emit --source-rebuild` for all four artifact
  declarations against the failed run snapshot and a candidate pool containing
  only the snapshot-authorized upstream packages. All four commands exited 0,
  selected `web-rest@0.7.0`, and reported empty `added`, `removed`, `sha_drift`,
  `signer_drift`, and `version_changed` evidence. The original unsatisfiable
  constraint is therefore resolved on the exact certification resolution mode.
- `git diff --check` and `just --list` are green.

The first attempt to run the source-rebuild check against the entire post-failure
run package directory was correctly rejected because it also contained
drift-workflows packages staged after the run snapshot was frozen. Repeating the
check with only the snapshot-authorized upstream candidate set is the applicable
pre-stage condition and passed. This was a verification-environment issue, not a
product defect.

## Required changes

### P1 — restore the unchanged package author claims

Restore `drift/microflows.author-claim` and `drift/singular.author-claim` to their
pre-work bytes. Neither artifact changed version, dependencies, or source content
identity. Re-dating and re-signing those sealed releases creates unrelated release
churn and must not be retained merely because `just reseal` loops over every
artifact. Keep the re-minted `drift/uflowsd.author-claim`; that artifact did change.

Use a non-mutating history/diff read to obtain the original one-line contents and
edit the files directly; do not use a mutating git restore operation.

### P2 — make the durable finding record agree with reproduced facts

Update `FINDING.md` and `PLAN.md` to remove or supersede the now-disproved claims
that `web-rest 0.7.0` was not published in any local package root and that the
migration was not built. Record the actual run package root, the completed
compile/integration evidence, and the successful four-artifact source-rebuild
check above. Preserve the history of the mistaken hypothesis if useful, but it
must be unmistakably labelled disproved rather than left under a `Confirmed`
heading. Update the finding status to the current awaiting-certification state.

`PROGRESS.md` remains implementer-owned and should be updated for this review
round, including the literal next action: reviewer re-check, then Slawomir's full
certification rerun. The repository stress/perf sweep is not required locally
before this re-review because the full certification rerun is the authority and
the machine-capacity announcements asked teams to avoid redundant heavy gates.

## Re-review boundary

No source, manifest, lock, uflowsd claim, changelog, test, or justfile behavior
change is requested. Re-review is limited to the two claim restorations and the
finding-record corrections. After those are clean, this implementation can be
signed off as ready for the certification rerun; terminal finding closure still
requires that rerun to pass.
