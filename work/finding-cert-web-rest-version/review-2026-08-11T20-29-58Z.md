# Review 2026-08-11T20:29:58Z — changes requested

Reviewer: `workflows.reviewer`

## Outcome

Changes requested. The diagnosis and consumer-surface evidence are sound, but
the plan currently treats a repository-policy decision as an external blocker
and omits required release-integrity work.

## Findings

### P1 — The sequencing question is already decided by repository policy

`AGENTS.md` states that this repository tracks `drift-foundation/lang-toolchain`
`main`, that the compatibility target is current `main`, and that a `main`
change which breaks this repository is immediate integration work. The rejected
certification run is the concrete staging overlay: it stages `web-rest 0.7.0`
from drift-web `558b647` before it stages drift-workflows. Waiting for 0.7.0 to
become certified would leave this same combined train unable to reach the
certification gates.

Resolve the Open item as: adopt `web-rest 0.7` now in this certification train.
Remove the Step 2 blocker and continue. This is not permission to fall back to
the old certified 0.6.5 package or to introduce a compatibility path.

### P1 — The release artifact cannot retain version 0.8.0

Changing the root `uflowsd` dependency declaration changes the artifact source
content identity. The already released/sealed `uflowsd 0.8.0` cannot be minted
again with different manifest content. The plan must include:

- bump root `uflowsd` from `0.8.0` to `0.8.1` (patch is appropriate if the
  build/integration evidence confirms no workflows-owned contract change);
- synchronize `RUNNER_VERSION` in
  `microflows/runner/src/runner.drift`;
- add a self-contained changelog entry explaining the 0.7 adoption;
- regenerate `drift/uflowsd.author-claim` and the root lock through the normal
  reseal path.

If verification finds an actual workflows contract change, re-evaluate the
version class rather than forcing the patch bump.

### P2 — Correct the lock-file count and expected diff

There are three lock files, not four:

- `drift/lock.json` — one `uflowsd` resolution entry;
- `microflows/runner/drift/lock.json` — two artifact resolution entries;
- `microflows/participant-stub/drift/lock.json` — one artifact resolution
  entry.

That is four `web-rest` pins across three generated files. Update `FINDING.md`,
`PLAN.md`, and implementer-owned `PROGRESS.md` accordingly. Relock against the
run's snapshot-gated 0.7.0 package inputs and stop if unrelated dependency pins
move.

### P2 — Reconcile the stale staging-policy comment

The root `justfile` says the orchestrator stages packages only and does not
stage `uflowsd`, while the rejected run invoked unfiltered `drift deploy` and
failed specifically while staging that app. Revalidate the orchestrator
contract. If app staging is now intentional, correct the comment in this work;
if it is not, file a nested finding rather than silently carrying contradictory
release instructions.

## Verification required for the next handoff

1. Snapshot-gated source-rebuild resolution for all four affected artifact
   entries selects `web-rest 0.7.0`.
2. `uflowsd`, `microflows-runner`, and participant-stub compile with zero
   migration source edits beyond `RUNNER_VERSION`, or any contrary compile
   evidence is recorded and root-caused.
3. Focused service integration covers the existing 503/draining and connection
   behavior on 0.7.0.
4. Root trust/reseal checks pass and the author claim matches the bumped
   manifest.
5. The repository's test/stress/perf gates pass in proportion to available
   machine capacity; the final certification rerun remains Slawomir's gate.

Do not edit existing tests merely to accommodate a failure. A compiler/runtime
correctness symptom would trigger the repository's `CORE_BUG` stop gate.
