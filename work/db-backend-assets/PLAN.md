# Superseded — see README.md

Per the `work/` charter (`work/README.md`), the effort plan lives in **README.md** (+ PROGRESS.md).
This `PLAN.md` is retained only as a redirect and should be deleted when implementation starts.
