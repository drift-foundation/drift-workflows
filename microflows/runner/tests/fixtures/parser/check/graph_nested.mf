args { mode: string, flag: bool }
op reserve { input: { reservation: string } }
steps { case mode { "a" { if flag { reserve { "reservation": "A" } } else { reserve { "reservation": "B" } } } default { reserve { "reservation": "D" } } } reserve { "reservation": "fin" } }
